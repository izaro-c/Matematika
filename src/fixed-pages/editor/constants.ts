/** Constantes editables del feature editor. */

/** Debounce de sincronización de estado de diagrama (ms). */
export const DIAGRAM_STATE_SYNC_DEBOUNCE_MS = 250;

/** Debounce del save coordinator (ms). */
export const SAVE_COORDINATOR_DEBOUNCE_MS = 500;

/** Order inicial de elementos nuevos en capa geometry. */
export const DEFAULT_NEW_ELEMENT_ORDER = 1000;

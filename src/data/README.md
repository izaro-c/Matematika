# `data/`

How the app reads `content/`: Zod schemas, ContentStore, graph JSON types.

| Folder | Role |
|---|---|
| `content/` | ContentStore, schemas, search, indexes |
| `graph/` | graph structure types + JSON |
| `metadata/` | Page metadata store (sidebar state) |

Metadata UI lives in `components/metadata/`.
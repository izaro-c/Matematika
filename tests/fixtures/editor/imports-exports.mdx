export const metadata = {
  "id": "imports-exports",
  "type": "teorema",
  "title": "Imports and Exports Test",
  "description": "Fixture checking imports and exports parsing",
  "authors": []
};

import { HelperComponent } from '@/shared/ui/Helper';
import { anotherUtility } from '@/shared/lib/util';

Un texto en el cuerpo con un componente de apoyo.

export const ComponentAdicional = () => <div>Exported component</div>;
export const SimulationConfig = { speed: 1.0 };

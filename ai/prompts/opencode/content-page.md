# OpenCode: página de contenido

Usa el agente `content-writer`. Lee `AGENTS.md`, `ai/current-state.md`, `ai/goals/pedagogy.md`, [`content-authoring`](../../context-packs/content-authoring.md) y carga `page-creator`.

Crea o modifica `[id, tipo y ruta]` para lograr `[objetivo pedagógico]`.

Antes de escribir, declara alcance, archivos permitidos/prohibidos, validaciones y resultado esperado.

Comprueba schema, plantilla, ID y dependencias reales. No inventes `leanId` ni amplíes a diagramas o Lean sin su skill y permiso. Entrega decisiones, archivos, validaciones y deuda.

# Codex: migración de token

Lee `AGENTS.md`, `ai/current-state.md`, `ai/goals/ui-product.md`, [`design-system`](../../context-packs/design-system.md) y los consumidores de `[token]`.

Migra `[token/patrón origen]` a `[token canónico]` únicamente en `[rutas]`.

Antes de escribir, declara alcance, archivos permitidos/prohibidos, validaciones y resultado esperado.

Inventaría coincidencias, separa definiciones canónicas de hardcoding visual y preserva modo oscuro, contraste y estados. No hagas reemplazos globales ciegos. Entrega consumidores migrados, remanentes y resultados.

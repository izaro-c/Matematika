# Optional editor API URL for production builds (GitHub Pages).
# When empty, the editor UI loads but cannot read/write remote content.
# Example: https://matematika-editor-api.fly.dev
VITE_EDITOR_API_URL=

# Bearer token entered in the editor UI for write operations (not baked into the build).
# Set the same value as EDITOR_API_TOKEN on the API server.

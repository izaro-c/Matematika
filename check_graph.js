import fs from 'fs';

const data = { nodes: [], links: [] };
// We will just read the TS code or I can just write a quick node script to check ContentStore.
// Actually I can just write a robust filter in GraphPage.tsx itself!

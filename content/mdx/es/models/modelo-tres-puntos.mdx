export const metadata = {
  id: "modelo-tres-puntos",
  lang: "es",
  type: "modelo",
  title: "Modelo de tres puntos",
  description: "Estructura finita minimal que satisface los axiomas de incidencia plana I.1, I.2 e I.3.",
  branch: "51A",
  satisfies: "sistema-incidencia",
  axioms_verified: ["axioma-incidencia-1", "axioma-incidencia-2", "axioma-incidencia-3"],
  hasDiagram: true,
  sources: [
    {
      title: "Grundlagen der Geometrie",
      author: "David Hilbert",
      locator: "Capítulo I, §2",
      role: "primary"
    }
  ]
};

import { ModeloTresPuntos } from '@content/diagrams/Models/ModeloTresPuntos';
export const Diagram = ModeloTresPuntos;

<Capitular letra="E" />l **modelo de tres puntos** es una <ConceptLink targetId="estructura-geometrica">estructura geométrica</ConceptLink> finita que satisface los tres primeros axiomas del <ConceptLink targetId="sistema-incidencia">sistema de incidencia</ConceptLink> plano de <ConceptLink targetId="david-hilbert">David Hilbert</ConceptLink>.

En <ConceptLink targetId="teoria-modelos">teoría de modelos</ConceptLink>, constituye un plano de <ConceptLink targetId="incidencia">incidencia</ConceptLink> minimal con <ConceptLink targetId="cardinalidad">cardinalidad</ConceptLink> finita, actuando como testigo semántico de la <ConceptLink targetId="consistencia-logica">consistencia lógica</ConceptLink> de los postulados de incidencia plana.

<Definicion title="Estructura formal del modelo">
El modelo se define como una tupla $\mathcal{M} = (\mathcal{P}, \mathcal{L}, \mathbf{I})$ compuesta por <ConceptLink targetId="conjunto-disjunto">conjuntos disjuntos</ConceptLink> y finitos ($\mathcal{P} \cap \mathcal{L} = \emptyset$):

- **<ConceptLink targetId="dominio">Dominio</ConceptLink> de <ConceptLink targetId="punto">puntos</ConceptLink>**: $\mathcal{P} = \{$ <VisualBind element="A" color="terracota">$A$</VisualBind>, <VisualBind element="B" color="terracota">$B$</VisualBind>, <VisualBind element="C" color="terracota">$C$</VisualBind> $\}$ con $A \neq B \land B \neq C \land A \neq C$.
- **<ConceptLink targetId="dominio">Dominio</ConceptLink> de <ConceptLink targetId="recta">rectas</ConceptLink>**: $\mathcal{L} = \{$ <VisualBind element="rAB" color="carbon">$\ell_{AB}$</VisualBind>, <VisualBind element="rBC" color="carbon">$\ell_{BC}$</VisualBind>, <VisualBind element="rCA" color="carbon">$\ell_{CA}$</VisualBind> $\}$.
- **<ConceptLink targetId="relacion">Relación</ConceptLink> de <ConceptLink targetId="incidencia">incidencia</ConceptLink>** $\mathbf{I} \subseteq \mathcal{P} \times \mathcal{L}$:
  $$
  \mathbf{I} = \{ (A, \ell_{AB}), (B, \ell_{AB}), (B, \ell_{BC}), (C, \ell_{BC}), (C, \ell_{CA}), (A, \ell_{CA}) \}
  $$
  o equivalentemente, mediante las trazas puntuales de cada recta en el <ConceptLink targetId="conjunto">conjunto</ConceptLink> de puntos: $\operatorname{tr}(\ell_{AB}) = \{A, B\}$, $\operatorname{tr}(\ell_{BC}) = \{B, C\}$ y $\operatorname{tr}(\ell_{CA}) = \{C, A\}$.
</Definicion>

<Separador />

<SeccionPropiedades>
  <PropiedadesGrupo title="Satisfacción de axiomas de incidencia">
    <PropiedadItem 
      id="axioma-incidencia-1" 
      title="Determinación unívoca de la recta"
      formula="\forall P, Q \in \mathcal{P} \ (P \neq Q \implies \exists! \ell \in \mathcal{L})"
    >
      Para cualquier par de puntos distintos en $\mathcal{P}$, existe exactamente una recta en $\mathcal{L}$ incidente con ambos ($\{A, B\}$ determina $\ell_{AB}$, $\{B, C\}$ determina $\ell_{BC}$ y $\{C, A\}$ determina $\ell_{CA}$), satisfaciendo el <ConceptLink targetId="axioma-incidencia-1">Axioma I.1</ConceptLink>.
    </PropiedadItem>

    <PropiedadItem 
      id="axioma-incidencia-2" 
      title="Puntos incidentes por recta"
      formula="|\operatorname{tr}(\ell)| = 2 \ge 2"
    >
      Toda recta $\ell \in \mathcal{L}$ incide con exactamente dos puntos distintos ($\lvert \operatorname{tr}(\ell) \rvert = 2 \ge 2$), satisfaciendo el <ConceptLink targetId="axioma-incidencia-2">Axioma I.2</ConceptLink>.
    </PropiedadItem>

    <PropiedadItem 
      id="axioma-incidencia-3" 
      title="Existencia de puntos no colineales"
      formula="|\operatorname{tr}(\ell)| = 2 < 3 \implies \exists \{A, B, C\} \text{ no colineales}"
    >
      Ninguna recta contiene simultáneamente a los tres puntos en su traza ($C \notin \operatorname{tr}(\ell_{AB})$, $A \notin \operatorname{tr}(\ell_{BC})$, $B \notin \operatorname{tr}(\ell_{CA})$); por consiguiente, existen ternas de puntos no <ConceptLink targetId="colinealidad">colineales</ConceptLink>, satisfaciendo el <ConceptLink targetId="axioma-incidencia-3">Axioma I.3</ConceptLink>.
    </PropiedadItem>
  </PropiedadesGrupo>

  <PropiedadesGrupo title="Propiedades estructurales y combinatorias">
    <PropiedadItem 
      id="regularidad-combinatoria" 
      title="Configuración simétrica y regularidad"
      formula="(3_2, 3_2)"
    >
      La estructura es homogénea y regular de tipo $(3_2, 3_2)$: posee cardinalidad $\lvert \mathcal{P} \rvert = 3$ y $\lvert \mathcal{L} \rvert = 3$, donde cada recta contiene exactamente 2 puntos y por cada punto concurren exactamente 2 rectas.
    </PropiedadItem>

    <PropiedadItem 
      id="ausencia-paralelas" 
      title="Ausencia de paralelismo"
      formula="\operatorname{tr}(\ell_i) \cap \operatorname{tr}(\ell_j) \neq \emptyset \quad (\forall i \neq j)"
    >
      Dos rectas distintas cualesquiera en $\mathcal{L}$ tienen trazas no disjuntas ($\operatorname{tr}(\ell_i) \cap \operatorname{tr}(\ell_j) \neq \emptyset$), intersecándose siempre en un único punto común; por tanto, el modelo carece de <ConceptLink targetId="paralelas">rectas paralelas</ConceptLink>.
    </PropiedadItem>

    <PropiedadItem 
      id="autodualidad" 
      title="Autodualidad"
      formula="\mathcal{P} \cong \mathcal{L}, \quad \mathbf{I} \cong \mathbf{I}^T"
    >
      La estructura es <ConceptLink targetId="dualidad">autodual</ConceptLink>: el intercambio formal de los dominios $\mathcal{P}$ y $\mathcal{L}$ junto con la <ConceptLink targetId="transposicion">transposición</ConceptLink> de la relación $\mathbf{I}$ genera una estructura de incidencia estrictamente <ConceptLink targetId="isomorfismo">isomorfa</ConceptLink> a la original.
    </PropiedadItem>
  </PropiedadesGrupo>
</SeccionPropiedades>